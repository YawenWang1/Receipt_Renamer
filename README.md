# Receipt_Renamer
**Receipt Renamer** — A clean and private AI-powered receipt scanner.  Automatically extracts date, vendor, amount, category, currency, and receipt number from PDFs and images using local OCR + LLM (Ollama). Intelligently renames files and exports a ready-to-use ZIP with summary CSV.
