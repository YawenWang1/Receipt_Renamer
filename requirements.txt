# Core
streamlit>=1.32.0

# Data & UI
pandas

# PDF and Image Processing
pymupdf                  # ← This was missing (fitz)
opencv-python-headless   # Better for cloud deployment # for python 3.13 compatibility issue
numpy

# OCR
paddleocr
paddlepaddle             # CPU version (safer for cloud)

# LLM Structured Output
pydantic
instructor
openai